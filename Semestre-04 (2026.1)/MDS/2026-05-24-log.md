# Log de Estudo — MDS · 24/05/2026

## Tempo de Estudo
4h

## Metas do Dia
- [x] Completar o dashboard de produtividade da equipe (SST)
- [x] Automatizar a coleta de métricas via GitHub Actions
- [x] Realizar o deploy da documentação no MkDocs

## Progresso
Dashboard 100% finalizado com 13 gráficos renderizados em D3.js (incluindo Heatmap e Code Churn). Pipeline de CI/CD (`update-metrics.yml`) corrigida e testada com sucesso localmente usando a ferramenta `act`. Deploy do MkDocs realizado na branch `gh-pages` contornando conflitos de histórico de forma segura (sem usar `--force`).

## Resumo / Anotações
Prática intensiva com a biblioteca D3.js para estruturação de gráficos complexos. Teste local de GitHub Actions no macOS usando OrbStack/Docker, manipulando flags de arquitetura (`linux/amd64`). Resolução de erros de sintaxe e indentação estrita em arquivos YAML. Estratégias avançadas de Git: contornar `non-fast-forward`, lidar com `upstream branches` e ressincronizar branches artificiais (`gh-pages`) deletando a referência local e fazendo `fetch`.

## Dificuldades Encontradas
O `act` apresentou bugs de certificado TLS e alertas de emulação devido ao chip Apple M1. O YAML do Actions falhou por desalinhamento de indentação. O Git bloqueou o push do MkDocs e da branch de CI devido a conflitos de estado com a nuvem, exigindo comandos de correção.

---
*Sessão registrada em 12:08*