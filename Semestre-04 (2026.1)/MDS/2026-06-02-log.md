# Log de Estudo — MDS — 02/06/2026

## Tempo de Estudo
40 minutos

## Metas do Dia
- [x] Implementar a métrica Tendência Recente (frontend only)
- [x] Implementar o Toggle "Dados Insuficientes" no ranking (frontend only)
- [x] Organizar o trabalho em commits atômicos

## Progresso
Todas as metas concluídas. Componente `TendenciaRecente.tsx` criado e integrado ao dossiê e à página de comparação. Toggle "Dados Insuficientes" implementado no `FilterBar` com lógica de filtragem na homepage. Trabalho organizado em 6 commits atômicos na branch `Teste_front`.

## Resumo e Anotacoes
**Tendência Recente**
- Calculada inteiramente no frontend a partir do array `TimelinePoint[]` já carregado nas páginas — zero chamadas extras à API
- Exibida apenas quando o parlamentar possui ao menos 15 votações analisadas, garantindo que as últimas 10 sejam representativas em relação ao histórico
- Formato: `↑ +5 pts nas últimas 10 votações` ou `↓ −8 pts nas últimas 10 votações`

**Toggle "Dados Insuficientes"**
- Estado gerenciado no `FilterBar`; parlamentares com `score_coerencia === null` ficam ocultos por padrão
- Com toggle ativado, aparecem após os demais — nenhum dado é escondido, satisfazendo a exigência do PO de transparência total

**Princípio transversal das métricas**
- Denominador explícito sempre visível junto ao valor: ex. `100% — 1 votação analisada`
- Nenhum percentual aparece sem contexto

**Métricas documentadas mas não implementadas (requerem backend)**
- Taxa de Presença: precisa de `total_votos` e `total_ausencias` no tipo `Parlamentar`
- Benchmark de Coerência: precisa de `media_partido` e `media_cargo` no tipo `Parlamentar`

**Comunicação frontend-backend**
- `lib/api.ts` já tem todas as chamadas implementadas contra `NEXT_PUBLIC_API_URL`
- Modo mock ativo (`NEXT_PUBLIC_USE_MOCK=true`) — para usar a API real basta mudar para `false` com o backend rodando via `docker compose up`

## Dificuldades Encontradas
Nenhuma

---
Sessao registrada em 17:00
