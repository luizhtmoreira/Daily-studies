# Log de Estudo — MDS — 06/06/2026

## Tempo de Estudo
3h

## Metas do Dia
- [x] Exibir resultados reais do pipeline de inferência RAG
- [x] Commitar pipeline de inferência (inferidor_postura, pipeline_inferencia, run_inferencia + testes)
- [x] Fazer push limpo da branch `feature/etl-extração`

## Progresso
Exibi os 2 resultados reais do pipeline: Sâmia Bomfim (PSOL-SP) foi classificada como coerente e Tarcísio Motta (PSOL-RJ) como incoerente. Commitei os 5 arquivos do pipeline de inferência via RAG. Identifiquei 6 commits de frontend indevidamente incluídos na branch (pertenciam à `origin/Teste_front`), removi-os via `git rebase --onto` e fiz o push com `--force-with-lease`.

## Resumo e Anotacoes
- Pipeline RAG completo: gero o embedding da proposição, busco os top-k chunks via RPC `buscar_discursos_similares` (com filtro temporal), envio ao LLM para inferir a postura FAVORÁVEL/CONTRÁRIO e comparo com o voto real para calcular `eh_coerente`
- `git rebase --onto <destino> <base>`: permite recortar um trecho do histórico e replantá-lo em outro ponto, descartando commits intermediários
- `--force-with-lease` é mais seguro que `--force`: aborta o push se o remote foi atualizado por outra pessoa entre o rebase e o push

## Dificuldades Encontradas
- A coluna `camara_politicos.nome_politico` não existia — os campos corretos são `nome_urna` e `partido`
- A branch tinha commits de outra feature vazados no histórico, exigindo rebase antes do push

---
Sessao registrada em 19:00
